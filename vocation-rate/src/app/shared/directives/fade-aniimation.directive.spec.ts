import { FadeAniimationDirective } from './fade-aniimation.directive';

describe('FadeAniimationDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeAniimationDirective();
    expect(directive).toBeTruthy();
  });
});
