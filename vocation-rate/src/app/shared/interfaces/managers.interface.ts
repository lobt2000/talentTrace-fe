export interface IManager {
  name: string;
  icon: string;
  active: boolean;
  id: number | string;
  firstName: string;
  lastName: string;
  email: string;
  position: string;
  permissions: any;
}
